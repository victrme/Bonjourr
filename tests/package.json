{
	"name": "tests",
	"private": true,
	"description": "Bonjourr Testing",
	"scripts": {
		"test": "pnpm build && npx playwright test",
		"ui": "pnpm build && npx playwright test --ui",
		"init": "npx playwright install",
		"build": "npx gulp buildtest"
	},
	"devDependencies": {
		"@playwright/test": "^1.41.2",
		"wrangler": "^3.28.2"
	}
}
