{
	"name": "tests",
	"description": "Bonjourr Testing",
	"scripts": {
		"test": "gulp buildtest && npx playwright test",
		"ui": "gulp buildtest && npx playwright test --ui",
		"api": "pnpm --filter api dev",
		"init": "npx playwright install && pnpm --filter api install",
		"build": "gulp buildtest"
	},
	"devDependencies": {
		"@playwright/test": "^1.41.2",
		"wrangler": "^3.28.2"
	},
	"keywords": [],
	"author": "Victor Azevedo",
	"license": "ISC"
}
